# ECE LAB ASSISTANT

## VERSION 2 — PROJECT SETUP AND BASIC FOUNDATION

Now we can begin building the project.

Follow all the rules and requirements established in Version 1.

For this stage, work ONLY on creating the basic project foundation.

Do NOT build the calculators yet.

Do NOT implement the Ohm's Law Calculator, Resistor Calculator, Number System Converter, Logic Gate Simulator, or RC Circuit Calculator yet.

Do NOT add features outside the requirements.

---

# 1. TASK FOR THIS STAGE

Set up the basic project structure and create the initial files needed for the application.

Create or confirm the following structure:

```text
ece-lab-assistant/
│
├── index.html
│
├── css/
│   └── style.css
│
├── js/
│   └── script.js
│
├── assets/
│   ├── images/
│   └── icons/
│
├── README.md
│
└── .gitignore
```

If Replit has already created any necessary project configuration files, do not delete or modify them unnecessarily.

Keep the project simple.

---

# 2. INDEX.HTML

Create the basic HTML5 structure.

Include:

* `<!DOCTYPE html>`
* `<html>`
* `<head>`
* character encoding
* viewport meta tag
* a suitable page title
* connection to `css/style.css`
* connection to `js/script.js`

Use a suitable title such as:

```text
ECE Lab Assistant | ECE Engineering Toolkit
```

Inside the `<body>`, create ONLY the basic semantic page structure.

Use:

```text
<header>
<nav>
<main>
<section>
<footer>
```

For now, create basic placeholder sections for:

```text
Home
Tools
About
```

Give important sections meaningful IDs where appropriate so that navigation can later connect to them.

Do not build the complete content of these sections yet.

The purpose of this stage is only to establish a clean HTML foundation.

---

# 3. BASIC HEADER AND NAVIGATION

Create a simple initial header.

Include:

### Application name

```text
ECE Lab Assistant
```

### Navigation links

```text
Home
Tools
About
```

The navigation links can connect to the appropriate sections using anchor links.

For example, clicking `Home` should connect to the Home section.

Keep the navigation simple.

Do not add complicated menus, dropdown navigation, JavaScript routing, or mobile navigation logic yet.

---

# 4. BASIC PAGE CONTENT

Create simple placeholder content so I can confirm that the page structure works.

For example:

## Home section

Display:

```text
ECE Lab Assistant
Your pocket toolkit for Electronics & Communication Engineering
```

## Tools section

Display:

```text
ECE Tools
Essential electronics tools will appear here.
```

## About section

Display a short placeholder description explaining that the application is a toolkit for ECE students.

Do not add calculator interfaces yet.

Do not create the final dashboard yet.

---

# 5. CSS FOUNDATION

Create `css/style.css`.

For this stage, add only the basic CSS foundation.

Include:

* basic reset or box-sizing
* body font settings
* basic background color
* basic text color
* basic spacing
* CSS variables for the main design colors
* simple readable header styling
* simple navigation styling
* basic section spacing

Use the design direction established in Version 1:

* modern
* clean
* technical
* professional
* minimal

Use a restrained engineering-inspired palette.

Do NOT spend time creating advanced visual effects yet.

Do NOT add excessive animations.

Do NOT use external CSS frameworks.

Keep the design foundation simple because the complete visual design will be improved in later stages.

Organize the CSS with clear comments.

---

# 6. JAVASCRIPT FOUNDATION

Create `js/script.js`.

For this stage, keep JavaScript minimal.

Do NOT implement any calculator logic.

For now, add only:

1. A clear comment explaining the purpose of the file.
2. A simple test to confirm that the JavaScript file is successfully connected.

For example, use a simple console message indicating that ECE Lab Assistant JavaScript has loaded.

Do not add unnecessary JavaScript.

---

# 7. README AND GITIGNORE

Create basic placeholder versions of:

```text
README.md
.gitignore
```

The README should only contain basic initial information such as:

* Project name
* Short project description
* Technologies used

Do not write the complete professional README yet.

The `.gitignore` should remain simple and appropriate for the actual project.

Do not invent unnecessary ignored files.

---

# 8. BEGINNER LEARNING REQUIREMENT

Before or after making the changes, explain briefly:

### HTML

How `header`, `nav`, `main`, `section`, and `footer` organize a webpage.

### CSS

What CSS variables are and why we use them for repeated colors.

### JavaScript

How the browser connects and loads the JavaScript file.

Keep explanations short and beginner-friendly.

---

# 9. TESTING

After creating the foundation, tell me exactly how to test it in Replit.

I should verify:

### Test 1 — Page loads

The page opens successfully in the Replit preview.

### Test 2 — CSS works

The page has the basic styling from `style.css`.

### Test 3 — JavaScript works

The browser console shows the JavaScript test message.

### Test 4 — Navigation works

Clicking:

```text
Home
Tools
About
```

moves to the correct section.

---

# 10. IMPORTANT RESTRICTIONS

For this stage:

DO:

* Create the basic project structure.
* Connect HTML, CSS, and JavaScript correctly.
* Create semantic HTML sections.
* Create basic styling.
* Create basic placeholder content.
* Keep the code beginner-friendly.
* Add useful comments.

DO NOT:

* Build the calculators.
* Build the resistor logic.
* Add advanced JavaScript.
* Add React or any framework.
* Add Node.js or a backend.
* Install packages.
* Add a database.
* Add authentication.
* Add unnecessary libraries.
* Create extra features.
* Generate a large application at once.

---

# 11. WHEN FINISHED

After completing this stage:

1. Summarize the files created or modified.
2. Explain the most important things I should understand.
3. Give me the Replit testing steps.
4. Give me a small testing checklist.
5. STOP and wait for my confirmation.

Do not move to the next development stage automatically.

# END OF VERSION 2
